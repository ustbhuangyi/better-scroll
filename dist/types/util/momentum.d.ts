export declare function momentum(current: number, start: number, time: number, lowerMargin: number, upperMargin: number, wrapperSize: number, options: any): {
    destination: number;
    duration: any;
};
